import wya from 'wya-js-sdk';
import Toasts from '@common/toasts/toasts';
import markdown from '@docs/consts/winWidth.md';

const invoke = () => {
	// ...
};

export default {
	to: '/consts/winWidth',
	title: 'winWidth',
	label: 'window 宽度',
	invoke,
	markdown
};
